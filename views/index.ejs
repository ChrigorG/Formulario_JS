<%- include('partils/header.ejs')%>
<body>
  <%- include('partils/navbar.ejs')%>
  <section>
      <div class="container"> <!-- Class container do boostrap-->
          <hr>
          <h1>Perguntas</h1>
          <a href="/perguntas" class="btn btn-primary">Perguntar</a>
          <hr>
          <%perguntas.forEach(perguntas =>{ %>
              <div class="card">
                  <div class="card-body">
                      <h4><%= perguntas.titulo %></h3> <!--Variavel coloca simbolo de '='-->
                  </div>
                  <div class="footer">
                      <button class="btn btn-success">Responder</button>
                  </div>                
              </div>
              <br>
          <% }) %>
      </div>
  </section>
  <%- include('partils/footer_descripton.ejs')%>
</body>
<%- include('partils/footer.ejs')%>

